"use client";
import React from "react";
import "@/components/Loading/Loading.css";

const AinboxLoading: React.FC = () => {
  return (
    <div className="fixed inset-0 w-screen h-screen bg-[#ffffff77] backdrop-blur-2xl flex justify-center items-center text-[64px] font-bold z-[999] colorScroll">
      ainbox
    </div>
  );
};

export default AinboxLoading;
