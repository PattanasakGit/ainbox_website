// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;


const nextConfig = {
    output: 'standalone',
    images: {
      domains: ['cdn.dummyjson.com'],
    },
  }
  
  export default nextConfig